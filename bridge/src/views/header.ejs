<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proxy</title>

</head>
<body>
  <header>
    <% if(!userInfo) { %>
    <a href="/login" >Login</a>
    <% } else { %>
    Signed in with <%= userInfo.login %>
    <a href="/logout" > Logout</a>
    <% } %>
  </header>


